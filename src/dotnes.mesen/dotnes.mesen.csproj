<Project Sdk="Microsoft.Build.NoTargets">
  <Import Project="../NuGet.props" />
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Description>Mesen Community Edition integration for .NES — downloads MesenCE at build time for 'dotnet run'. The integration is MIT-licensed; the separately downloaded emulator is GPLv3.</Description>
    <PackageProjectUrl>https://github.com/nesdev-org/MesenCE</PackageProjectUrl>
    <PackageTags>$(PackageTags) Emulator Mesen MesenCE</PackageTags>
    <PackageVersion>2.2.1</PackageVersion>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <NoWarn>$(NoWarn);NU5119;NU5128</NoWarn>
    <GenerateDependencyFile>false</GenerateDependencyFile>
    <BeforePack>
      _AddPackageFiles;
      $(BeforePack);
    </BeforePack>
  </PropertyGroup>
  <ItemGroup>
    <None Update="@(None)" Pack="true" PackagePath="" />
    <ProjectReference Include="..\dotnes.tasks\dotnes.tasks.csproj" ReferenceOutputAssembly="false" />
  </ItemGroup>
  <Target Name="_AddPackageFiles">
    <ItemGroup>
      <_PackageFiles Include="..\dotnes.tasks\bin\$(Configuration)\netstandard2.0\dotnes.tasks.dll" PackagePath="build" />
    </ItemGroup>
  </Target>
</Project>
